import React from "react"
import Layout from '../components/layout';

const Home = () => {
    return (
        <Layout>
            <div className="bg-blue-500">Hello</div>
        </Layout>
    );
};

export default Home;